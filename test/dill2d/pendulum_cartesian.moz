include DILLEXPORT

def g = 9.81
def x, y, vx, vy, l, th: Real
def p = x' = vx; y' = vy; vx' = x * l; vy' = y * l - g; x^2. + y^2. = 1.
  ; x <- 1.
  ; l <- 0.

def main = {
  exportDill2Modelica "Cartesian_Pendulum" p
}
