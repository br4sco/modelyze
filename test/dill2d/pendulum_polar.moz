include DILLEXPORT

def g = 9.81
def x, y, om, th, vx, vy, ax, ay, al: Real
def m = th' = om; al = -g * cos th
  ; x = cos th; y = sin th; x' = vx; y' = vy; vx' = ax; vy' = ay; om' = al
  ; th <- 0.

def main = {
  exportDill2Modelica "Polar_Pendulum" m
}
