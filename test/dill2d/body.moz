include DILLEXPORT
include DILL2DV

def m = 1.
def I = 1.
def l = 2.

def sqr(c: Color, h: Real) = visSolid (rectangle h 1.) (stdMaterial c 0.7)
def circ(c: Color, r: Real) = visSolid (circle r) (stdMaterial c 0.7)

def DATUM, f_1, f_2, f_3: Frame

def mes = Union [
                   vis (bodyFun 1. 1.) (sqr BLUE 1.) DATUM f_1 f_2,
                   rigidArmFun 2. 0. DATUM f_2 f_3,
                   vis (bodyFun 1. 1.) (sqr GREEN 1.) DATUM f_1 f_3
                 ]

def main = {
  // dprintln (filterVisElements (Set.toList mes))
  elaborateToFile (Set.toList mes)
}
