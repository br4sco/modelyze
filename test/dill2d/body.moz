include DILLEXPORT
include DILL2DV

def n_1, n_2: Node

def x, y, th: Real
def rr = vec x y

def vx, vy, om: Real
def vv = vec vx vy

def m = 1.
def I = 1.

def b = visualize (visualSolid (rectangle 1. 1.) (stdMaterial BLUE 0.7)) n_1
        ++ modelCpnt (Body m I  rr vv th om DATUM n_1)
        ++ modelCpnt (om <- 0.; vy <- 0.1)

def main = {
  elaborateToFile b
  // println (exportDill2Modelica "Falling_Body" (getModel o))
}
